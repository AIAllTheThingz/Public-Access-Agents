# Validation Tools

The tools use the Python standard library and are intended to run locally and in CI.

```bash
python tools/validate-standards/validate_repository.py
python tools/check-links/check_links.py
```

The tools detect structural problems. They do not prove that a standard is secure, correct, or complete.
