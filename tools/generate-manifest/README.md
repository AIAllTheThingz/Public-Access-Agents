# Generate Project Manifest

Planned tool for producing a `project-manifest.json` that validates against `schemas/project-manifest.schema.json`.

Until implemented, copy an example manifest and edit it manually.
