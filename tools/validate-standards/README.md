# Validate Standards Repository

Runs structural checks using the Python standard library.

```bash
python tools/validate-standards/validate_repository.py
```

Checks include required root files, JSON parsing, duplicate front-matter IDs, and accidental placeholder `AGENTS.md` files.
