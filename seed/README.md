# Temporary Seed Payload

This directory contains the one-time compressed repository payload used to seed the public standards library. The expansion workflow deletes this directory after successful extraction.

Trigger state: merge-triggered expansion.
