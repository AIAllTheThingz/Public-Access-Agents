# Minimal Composition Example

This example demonstrates a standards composition using fictitious metadata.

Files:

- `project-manifest.json`

A real project would copy the selected standards into its repository, add project-specific constraints, and validate the resulting links and manifests.
