name: Build platform packages

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out pull request branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          fetch-depth: 0

      - name: Build platform packages
        shell: bash
        run: |
          if [[ -f bootstrap/platforms/build.py ]]; then
            python bootstrap/platforms/build.py
          else
            echo "Platform packages are already generated; nothing to do."
          fi
